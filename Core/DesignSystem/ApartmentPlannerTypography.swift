import SwiftUI

enum ApartmentPlannerTypography {
    static let screenTitle = Font.system(.largeTitle, design: .default).weight(.semibold)
    static let body = Font.system(.body, design: .default)
    static let secondaryBody = Font.system(.callout, design: .default)
    static let badge = Font.system(.caption, design: .default).weight(.semibold)
}

